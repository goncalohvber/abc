int main(void) {

    setlocale(LC_ALL, "Portuguese");
    
    FILE *F1=NULL;
    F1=fopen("configfeita.txt", "r");
    Confparque config;
    if(!F1) {
        personalizapp(config);
    }
    else{
        mostrarMenu();
    }
}
